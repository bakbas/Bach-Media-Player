{
  "name": "@bach/ui",
  "version": "0.0.0",
  "description": "Bach Media Player UI controls: play, progress, volume, time, fullscreen. Slot-based, ::part()-styled, ARIA-correct.",
  "license": "MIT",
  "type": "module",
  "sideEffects": ["./src/define.ts", "./dist/define.js", "./dist/define.cjs"],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    },
    "./define": {
      "types": "./dist/define.d.ts",
      "import": "./dist/define.js",
      "require": "./dist/define.cjs"
    }
  },
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "README.md", "CHANGELOG.md"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:browser": "vitest run --browser",
    "coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "size": "size-limit",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "peerDependencies": {
    "@bach/core": "workspace:*"
  },
  "dependencies": {
    "@preact/signals-core": "^1.8.0"
  },
  "devDependencies": {
    "@bach/core": "workspace:*"
  },
  "size-limit": [
    {
      "name": "@bach/ui (ESM, gzipped)",
      "path": "dist/index.js",
      "limit": "10 KB"
    }
  ],
  "publishConfig": {
    "access": "public"
  }
}
